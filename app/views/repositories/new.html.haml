%main.container.mx-auto.px-4.py-8.max-w-2xl
  .bg-white.rounded-lg.shadow-sm.p-8
    %h2.text-2xl.font-bold.mb-2.text-center 新規解析

    = form_with model: @repository, class: 'space-y-6' do |f|
      - if @repository.errors.any?
        .bg-red-100.border.border-red-400.text-red-700.px-4.py-3.rounded
          %ul.list-disc.list-inside
            - @repository.errors.full_messages.each do |message|
              %li= message

      .space-y-2
        = f.label :url, 'リポジトリURL', class: 'block text-sm font-medium text-gray-700'
        = f.text_field :url, placeholder: 'https://github.com/owner/repo', class: 'w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent', required: true

      = f.submit '解析する', class: 'w-full bg-blue-600 text-white py-3 px-4 rounded-lg hover:bg-blue-700 transition-colors cursor-pointer font-medium'
